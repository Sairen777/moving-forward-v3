import { quietFieldBackground } from "./quiet-field/profile";

import backgroundStyles from "./quiet-field/background.module.css";

export const activeBackground = quietFieldBackground;
export const activeBackgroundMeta = activeBackground.meta;
export const activeBackgroundStyles = backgroundStyles;
